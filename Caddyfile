# Cesar OS — reverse proxy con HTTPS automático
# Fase 1: el dominio principal sirve Open WebUI (tu chat). llm.* y observ.* siguen igual.

{$DOMAIN} {
	reverse_proxy open-webui:8080
}

llm.{$DOMAIN} {
	reverse_proxy litellm:4000
}

observ.{$DOMAIN} {
	reverse_proxy langfuse:3000
}
